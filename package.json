{
  "name": "ai-study-assistant-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root runner for AI Study Assistant",
  "scripts": {
    "dev": "npx concurrently --kill-others -n \"BACKEND,FRONTEND\" -c \"pink,violet\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend/ai-learning-assisant\""
  }
}
